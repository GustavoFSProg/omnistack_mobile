{
  incidents.map((incident) => (counter = counter + 1))
}

<Text>Total de Casos{incidents.map((incident) => (counter += 1))}
<Text style={styles.headerTextBold}>
  {' '}
  <Text>{counter}</Text> <Text>casos</Text>
  </Text>
  </Text>